# Progress Tracker

Update after every completed feature. Anyone (or any agent) reading this should immediately know what is done, what is next, and what is unresolved.

---

## Current Status

**Phase:** Phase 3 — The Launchable Site (launch gate)
**Last completed:** 09 Writing Index + Post Pages (+ first post)
**Next:** 10 About Page

---

## Progress

### Phase 1 — Foundation
- [x] 01 Project Setup
- [x] 02 Design System Foundation
- [x] 03 Layout Shell

### Phase 2 — Content Pipeline
- [x] 04 MDX Pipeline + Content Layer
- [x] 05 Content Components

### Phase 3 — Launchable Site (launch gate)
- [x] 06 Hero
- [x] 07 The Ledger
- [x] 08 Case Study Pages (SubTrack + WithinTech Learn)
- [x] 09 Writing Index + Post Pages (+ first post)
- [ ] 10 About Page
- [ ] 11 SEO, Metadata, OG Images
- [ ] 12 Deploy + Custom Domain

### Phase 4 — Depth (post-launch)
- [ ] 13 Restaurant Platform Case Study
- [ ] 14 Post 2 — Append-only financial records
- [ ] 15 Post 3 — Governing an AI coding agent
- [ ] 16 Scroll Reveal

---

## Open Questions

Blocking content, not code. Resolve before the features that need them.

- [x] **Domain name** — `brucenkundabagenzi.com`. Buy it early; DNS propagation is not instant
- [x] **Display name** — confirmed: `Bruce Nkundabagenzi`
- [x] **Years of experience** — resolved: no years figure on the site. Pre-Sept-2025 was university work, hackathons, and internships; the site claims scope instead of tenure (see `profile-facts.md`)
- [x] **Internship details** — CreativeTim (May–Jun 2024) and TV1 (Jan–Jul 2025) recorded in `profile-facts.md`; CV only, not the site. Creative Tim confirmed as creative-tim.com — spell it as two words
- [x] **Real figures for the Ledger** — resolved. Entry 01 uses system scale (28 modules, 3 apps/1 API, 53 issues remediated, sole engineer); the customer count (3) stays off the site with a prepared interview answer. Entry 02 uses platform figures from WithinTech's own landing page
- [x] **WithinTech ownership calibration** — resolved. Course structure, quizzes, email engagement, i18n, and dark theme are his; the code-execution engine, challenges, and auth are **not** and are removed from all copy. Payments are "integrated/configured", not "built". Full table in `profile-facts.md`
- [x] **Timezone overlap claim** — computed in `profile-facts.md` (full European day, ~7h US Eastern); confirm sustainability before publishing
- [x] **MDX pipeline choice** — resolved: `next-mdx-remote/rsc`. It matches the already-speced `lib/content.ts` loader (fs read → gray-matter → zod → compile) far better than `@next/mdx`'s bundle-time import model, which doesn't suit a dynamic `[slug]` content collection. Full rationale in `architecture.md`
- [x] **Toggle change announcement** — resolved: `aria-label` update only, no `aria-live` region. Focus stays on the button through the click, which is the reliable case for a re-announced accessible name; a live region would risk double-announcing the same change. Rationale in `ui-rules.md`
- [x] **Restaurant platform disclosure review** — resolved. Bruce reads it first, the deployment engineer reviews second before it publishes

---

- [x] **Screenshot plan** — full shot list per project in `screenshot-spec.md`. Existing SubTrack shots need retaking (browser chrome, personal tab titles, seed-data issues)
- [x] **Client approval for restaurant-platform screenshots** — obtained. Condition: never name the client or the internal system. Full boundary (including Bruce's own added restrictions on real data and vulnerability detail) in `screenshot-spec.md`
- [ ] **WithinTech dark-theme screenshot needs recapture** — `public/work/withintech-learn/light-theme.png` and `dark-theme.png` were meant to be the "same view in both themes" shot from `screenshot-spec.md`'s shot list, but both actually show the **Challenges admin list** (title/difficulty/status/attempts columns, admin sidebar), not something Bruce built — `screenshot-spec.md` explicitly says "Never capture... the challenge list" and `profile-facts.md`'s ownership table marks Challenges as not his. Found and flagged during feature 08; user decided to recapture a proper light/dark pair of an owned view (e.g. course browsing or dashboard) later rather than block the case study on it now. The WithinTech Learn case study currently ships without a dedicated theme-comparison shot — dark mode is still visible via the other (already-dark) screenshots. Swap in the real pair before feature 12 launch, and delete the two current files once replaced so they don't get used by accident.

---

## Decisions

- **Site title confirmed:** `Software Engineer` on the site, `Digital Apps Developer (Software Engineer — full stack + mobile)` on the CV
- **Domain purchased:** `brucenkundabagenzi.com` on Namecheap, $11.48 first year, Domain Privacy included free/forever, auto-renew on. DNS not yet pointed at Vercel — do this at feature 12 (or earlier if you want zero propagation delay at launch)

- **CV PDF: confirmed yes** — `resume.pdf` in `public/`, linked from the header and About page (see `architecture.md`)
- **Role positioning: full-stack leads, mobile stated explicitly, backend depth proven via case studies rather than led with** — reasoning in `profile-facts.md`'s Role Positioning section
- **Site stays quiet on job-search status** — no “open to work” banner; Bruce is mid-contract on both current engagements and a public banner could unsettle either relationship

- **Next.js 16 + MDX + static export, no CMS** — content is versioned with the code; the site is a work sample in the stack being claimed
- **Fresh identity, not SubTrack's** — teal/Inter/shadowed cards belong to the product being showcased, not the showcase
- **The Ledger as signature section** — the double-entry metaphor encodes the actual engineering thesis (systems where parts must agree) rather than decorating it
- **Dark mode ships in v1, designed upfront** (reversed from the initial plan at the user's call — correctly). Rationale: retrofitting a second theme is painful because components accumulate hardcoded light-mode values, so designing both now forces a two-layer semantic token system that prevents the leak entirely. Both palettes land in feature 02; the toggle lands in feature 03
- **Theme toggle: three states (`system → light → dark`), cycling, icon + mono label** — resolved, no longer open. Fixed button width sized to the longest label so the header never reflows on click; label hides below 640px with the accessible name carrying full state. Spec in `ui-rules.md`
- **Themes are a designed pair, not an inversion** — dark is warm plum-shifted (never neutral black), gold lifts to `#D4A24C` for contrast, and the feature section inverts in opposite directions per theme (darker on light, lighter on dark) through one semantic token
- **Launch at feature 12, not feature 16** — the job search starts when the link works
- **`next.config.ts` sets `output: "export"` and `images.unoptimized: true` from feature 01, not deferred to launch** — static export disables Next's Image Optimization server outright, so leaving `next/image` on its default loader would build clean now and fail the moment `output: "export"` is added later. Setting both together at project setup means every subsequent feature is developed against the real static-export constraint
- **`playwright` (core library) added as a dev dependency** — lets an agent drive a real Chromium instance for the visual/contrast/keyboard/reduced-motion checks each feature's verify step calls for, instead of relying on the human to eyeball every pass. Recorded in `architecture.md`'s Dependency Policy; ad-hoc scripts only, no committed test suite
- **Light-mode `--gold` corrected from `#A8792B` to `#8A6323`** — found during feature 02's Playwright verification: the original value measured 3.33:1 against `--surface`, short of the ~4.6:1 `ui-tokens.md`'s own contrast table always claimed and short of WCAG AA (4.5:1) for the inline-prose links `ui-rules.md` specs at body text size. `#8A6323` (same hue/saturation, darker) measures 4.65:1, matching the ratio the doc already claimed — likely the originally intended value. Updated in both `ui-tokens.md` and `globals.css` in the same pass

---

## Session Notes

- Context docs (`project-overview`, `content-spec`, `architecture`, `ui-tokens`, `ui-rules`, `motion-spec`, `code-standards`, `build-plan`, `git-workflow`) written before any code, deliberately — same approach that kept SubTrack coherent across 30+ sessions
- `profile-facts.md` added as the single source of truth for personal facts; `content-spec.md`, `project-overview.md`, `architecture.md`, and `build-plan.md` updated to reference it and to drop the employer's internal system name (client cannot be named — the case study slug is `restaurant-saas`)
- `git-workflow.md` is carried over from SubTrack with only the project name changed; conventions are identical
- Dark mode was added to the plan after the first draft of these docs; `ui-tokens.md` (color system), `ui-rules.md` (toggle, dark-mode screenshots), `architecture.md` (flash prevention, no-JS fallback), `build-plan.md` (features 02/03/12), and `code-standards.md` (semantic-token rule, both-themes verification) were all updated in the same pass — no doc left describing a single-theme site
- **02 Design System Foundation:** both palettes, semantic layer, Tailwind v4 `@custom-variant dark` wiring, type scale, spacing/radius/motion tokens, fonts (Fraunces w/ SOFT+WONK axes, Archivo, IBM Plex Mono via `next/font/google`), base element styles, focus ring, reduced-motion block, and the no-JS `@media (prefers-color-scheme: dark)` fallback all landed in `globals.css` + `layout.tsx`. The no-JS fallback selector is `:root:not(.light):not(.dark)` rather than the simpler `:not(.dark)` — feature 03's inline theme script must add an explicit `.light` class when the resolved theme is light (not just omit `.dark`), otherwise a manual "light" override on a dark-OS device would be clobbered by the fallback media query. Verified with Playwright (see decision above) rather than manual screenshots — contrast, keyboard focus, 320px overflow, and reduced-motion all confirmed in both themes; scratch page and script deleted after
- **03 Layout Shell + Theme Toggle:** `Header` (sticky, wordmark, nav, toggle), `Footer`, `Container`, `not-found.tsx`, root `layout.tsx` with the blocking inline theme script, `viewport.themeColor` (media-query pair, per Next 16's current API — `themeColor` lives in the `viewport` export, not `metadata`), and root `metadata` (`metadataBase`, title template, description). `lucide-react` installed (pre-approved in `architecture.md`).
  - **Inline script always resolves to exactly one class** (`.dark` or `.light`), even when the user's choice is "system" — it reads `localStorage.theme`, falls back to `matchMedia`, and sets the class before paint. This means the CSS `:root:not(.light):not(.dark)` fallback only ever fires for JS-disabled visitors, which is its intended purpose. `ThemeToggle`'s `applyTheme()` mirrors this exact logic so client-side toggling and the pre-hydration script never disagree.
  - **`ThemeToggle` reads its initial state via a `useState` lazy initializer**, not a `useEffect` — the initializer runs during the client's first render (before that render is ever painted), so there's no separate render-then-correct pass. `suppressHydrationWarning` is on the icon/label spans since the server-rendered default (`"system"`/`Monitor`, since the server has no `localStorage`) will usually differ from the client's real stored value.
  - **A third and fourth client component were needed beyond the toggle**: `NavLinks` (uses `usePathname()` for active-route highlighting — not available to Server Components) and `ThemeToggle` itself. `Header`/`Footer`/`Container` stay Server Components.
  - **Header's "hairline appears only after scroll" is CSS-only** — a scroll-driven animation (`animation-timeline: scroll(root)`) fades the border-bottom in from transparent over the first 48px of scroll, wrapped in `@supports (animation-timeline: scroll())` with a static hairline as the fallback for non-supporting browsers. This avoids a third scroll-listening client component. Confirmed working with Playwright (border-color changes from `transparent` to `--rule` once real scrollable content exists — the feature 03 placeholder home page has none yet, so this needs a recheck once feature 06/07 add real page height).
  - **320px pass failed on the first pass**: the full wordmark "Bruce Nkundabagenzi" plus three nav links plus the toggle measured 423px wide against a 320px viewport. Fixed by showing "BN" as the wordmark below the `640px` breakpoint (same breakpoint the toggle already uses to hide its label) and tightening nav/toggle gaps at that width. Full name still shows at ≥640px.
  - **Meta description was authored now** (154 chars, trimmed from the hero subhead already approved in `content-spec.md`) so `layout.tsx`'s root metadata isn't left empty — revisit at feature 11 once the Hero and per-page metadata exist.
  - **`NEXT_PUBLIC_SITE_URL` set** to `https://brucenkundabagenzi.com` in local `.env` (untracked) and recorded in a new committed `.env.example`.
  - Verified with Playwright: OS-default (light/dark) with `localStorage` cleared, zero white flash across 5 dark-mode reloads, keyboard tab order + focus-visible in both themes, toggle cycles `system → light → dark → system` with updating `aria-label` and no header reflow, 320px overflow-free in both themes, reduced-motion content visible, 404 page renders with header/footer. Scratch script and screenshots deleted after.
- **04 MDX Pipeline + Content Layer:** `src/lib/content.ts` (fs read → `gray-matter` → `zod` validation → `next-mdx-remote/rsc`'s `compileMDX`, per the decision already recorded in `architecture.md`) and `src/lib/reading-time.ts` (hand-rolled word-count estimate, no package — none was on the approved dependency list and the math is trivial). `gray-matter`, `zod`, `shiki`, `next-mdx-remote` added to `package.json`, matching the pre-approved list in `architecture.md`'s Dependency Policy — no new entry needed there.
  - **`compileMDX` is called inside the `lib/content.ts` loaders** (`getWorkBySlug` / `getPostBySlug`), not in the page components — the architecture doc's own wording ("`compileMDX()` slots directly into that loader") settled this. Both loaders accept an optional `components` map merged with a built-in one, so feature 05/08/09 can layer on the Content Components' MDX overrides (headings, prose, `Figure`, etc.) without touching the compile call itself.
  - **Shiki is wired via a `pre` component override** (an async Server Component that reads the code element's language + text and calls `codeToHtml`), not a rehype plugin — this avoids adding `@shikijs/rehype` or `unist-util-visit` (neither on the approved dependency list) for what a ~10-line component override does just as well under RSC. Verified end-to-end: a fixture code block in `content/work/subtrack.mdx` produces real `class="shiki ..."` markup in the static HTML output of a temporary scratch page (deleted after, same pattern as feature 02's scratch page).
  - **Theme picked for now: `github-dark`, unreconciled with `--surface-code`.** `ui-rules.md` specifies code blocks are theme-stable and sit on the `--surface-code` token — feature 05, which owns the actual code-block Content Component and its visual treatment, needs to either strip Shiki's inline background so `--surface-code` shows through, or swap to a Shiki theme/`createCssVariablesTheme()` setup that resolves to the same token. Flagging so it isn't missed.
  - **Content fixtures created for all three case studies and two posts** (`content/work/{restaurant-saas,withintech-learn,subtrack}.mdx`, `content/writing/{append-only-financial-records,correct-daily-job-hosting-sleeps}.mdx`). Frontmatter fields (title, summary/description, role, timeframe, stack, links, figures, order) are sourced only from already-approved text in `content-spec.md` and `profile-facts.md` — nothing invented — but the MDX **bodies are placeholders** pointing at the build-plan feature that owns the real write-up (08 for the two non-confidential case studies, 13 for the restaurant platform after client review, 09/14 for the two posts). Both posts are `draft: true` since neither is written yet; this also exercised the draft-filtering logic (`next build`'s static generation runs with `NODE_ENV=production`, and the writing index came back empty, correctly excluding both drafts).
  - **Verification used `next build` against a temporary `src/app/scratch-content/page.tsx`**, not a standalone `tsx`/`ts-node` script — running the loader through plain `tsx` hit an unrelated ESM/CJS resolution error in a transitive dependency (`estree-walker`, pulled in by the MDX compiler) that doesn't occur under Next's own bundler. The build-based check is arguably the more authoritative one anyway (it's the real pipeline, Turbopack and all). Confirmed: the parsed index of both collections printed correctly (work sorted by `order`, drafts excluded from the writing index in production), and deliberately setting `order: "three"` in `subtrack.mdx` failed the build with a readable, file-and-field-specific error (`Invalid frontmatter in content\work\subtrack.mdx:\n  - order: Invalid input: expected number, received string`) before it was reverted. Scratch page and build output deleted after.
  - No UI shipped in this feature (no page renders these loaders in production yet), so the usual keyboard/reduced-motion/both-themes/320px checks don't apply — first real applicability is feature 08.
- **06 Hero:** `src/components/home/Hero.tsx` (eyebrow, headline, subhead, CTA row — copy verbatim from `content-spec.md`) plus a new `src/components/ui/Button.tsx` (`primary`/`secondary` variants per `ui-rules.md`'s Buttons table; the tertiary mailto link is a plain `<a>` inline in `Hero.tsx`, matching `Footer.tsx`'s existing convention of not routing non-navigable links through `next/link`). Wired into `src/app/page.tsx`, replacing the feature-03 placeholder.
  - **The one hero moment is pure CSS**, added to `globals.css` as `@keyframes hero-in` plus three classes (`.hero-headline`, `.hero-subhead`, `.hero-ctas`) with staggered `animation-delay` (0/70/140ms), same pattern as feature 03's `theme-icon-in`/`reveal-hairline` one-off animations. No JS controls visibility, so the text is present and readable with JavaScript disabled by construction — the animation only ever adds motion on top of already-rendered content. Duration 250ms per element keeps the full sequence (last element's delay + duration) at 390ms, under the 400ms `--motion-slow` ceiling `motion-spec.md` specifies. The eyebrow does not animate — `motion-spec.md`'s hero section explicitly lists only "the headline, subhead, and CTA row."
  - **Verified with Playwright**: JS-disabled context confirms the h1, subhead, and CTAs are all present in the DOM; reduced-motion context confirms every animated element resolves to `opacity: 1` (not stuck at 0); keyboard tab order reaches all three CTAs with a visible focus outline; both-themes computed styles confirm the primary button resolves to `--text-primary` background / `--surface` text and the secondary button's border resolves to `--text-primary`, exactly matching the token values in both themes (no hardcoded color leaked); no horizontal overflow at 320px in either theme; LCP element confirmed as the `<h1>`. Scratch script and screenshots deleted after.
- **05 Content Components:** `src/components/content/mdx-components.tsx` (the `mdxComponents` map: `h2`/`h3`/`h4`, `p`, `ul`/`ol`, `blockquote`, `a`) plus three standalone components — `Figure.tsx` (next/image + mono caption, wrapped in the `--surface-raised`/`--rule` frame from `ui-rules.md`'s screenshot-in-dark-mode treatment, applied to every image rather than conditionally, since the frame is harmless in light mode and the component layer can't branch on theme anyway), `StackChips.tsx`, `MobileScreenshotFrame.tsx` (horizontally-scrollable row, `[&>figure]` child selector fixes each `Figure` to ~280/320px regardless of its own intrinsic image size). `Figure`/`StackChips`/`MobileScreenshotFrame` are exported from `mdx-components.tsx` alongside the tag overrides so case-study/post bodies can use them as JSX shortcodes directly in `.mdx` source (e.g. `<Figure src="…" caption="…" />`), not just from page-level TSX.
  - **`next-mdx-remote` 6+ defaults `blockJS: true`, which silently strips every `{expression}`-valued JSX attribute** — not just executable code, but numeric literals (`width={1894}`) and array literals (`stack={[...]}`) too. First build of the fixture failed with `StackChips` receiving `stack: undefined`; isolated with a throwaway script before finding the cause in `next-mdx-remote`'s own docs. Since all MDX content is authored solely by Bruce and versioned in the repo (never user-submitted), `options: { blockJS: false }` was added to both `getWorkBySlug` and `getPostBySlug` in `lib/content.ts` — `blockDangerousJS` stays at its default `true`, so `eval`/`Function`/`process`/`require` access is still blocked. Recorded here since it's the kind of silent-failure gotcha that would otherwise resurface confusingly in feature 08/09.
  - **Resolved the feature-04 flag on the Shiki/`--surface-code` mismatch**, in `lib/content.ts` rather than a feature-05 component, since `CodeBlock` (the `pre` override) already lives there and a second `pre` entry in a components map would silently clobber it (the merge order is `{ pre: CodeBlock, ...components }`). Used Shiki's `transformers` option: a `pre(node)` hook deletes the theme's inline `style` (which set a background/color that didn't match the token) and adds the token-backed utility classes (`bg-surface-code`, `rounded-lg`, `overflow-x-auto`, `text-small`) via `this.addClassToHast`. Token-level span colors from the `github-dark` theme are left untouched — only the outer surface needed to match the token, and per-token syntax colors reading as "code, regardless of site theme" is the point.
  - **Verified with Playwright** against a temporary `content/work/_scratch-content-fixture.mdx` + `src/app/scratch-content-components/page.tsx` (same disposable-fixture pattern as features 02/04): computed-style checks confirmed the code surface resolves to `#1F1A24` in both themes, prose links resolve to `--accent` (not a coincidentally-similar color) with correct external-link `target`/`rel`, and blockquote color/border match `--text-secondary`/`--accent`. Keyboard tab order reaches both prose links with a visible 2px accent outline; the Shiki `<pre>`'s own `tabindex="0"` and the `MobileScreenshotFrame`'s scrollable row are both real Chromium-native focus stops for horizontally-scrolling content, not something added here. No horizontal overflow at 320px in either theme; `prefers-reduced-motion` requires no special handling since none of these components carry entrance motion, only a hover-only underline-thickness transition already covered by the site-wide reduced-motion block. Fixture, scratch page, and screenshots deleted after.
- **07 The Ledger:** `src/components/home/Ledger.tsx` (section + `LedgerEntry`), rendered on `page.tsx` below `Hero`. Driven entirely by `getAllWork()` from `lib/content.ts` (already sorted by `order`) rather than a hardcoded array, per the build-plan requirement — the three entries render in the same order as `content-spec.md`'s Ledger section (restaurant platform, WithinTech Learn, SubTrack) because that's the existing `order` value in each MDX file's frontmatter.
  - **`built` and `proves` added as new required frontmatter fields** on the work schema (`src/lib/content.ts`), plus an optional `liveLinkLabel` paired with the existing `liveUrl`. The Ledger's Built/Proves prose is real per-entry copy from `content-spec.md`, not boilerplate — `code-standards.md`'s "copy lives in content-spec.md and MDX files, never inline in component JSX" ruled out hardcoding it in the component, and it isn't derivable from the existing `summary` field, so it needed its own frontmatter fields. All three `content/work/*.mdx` files updated with the exact `built`/`proves`/`liveLinkLabel` text from `content-spec.md` — nothing invented. `liveLinkLabel` is deliberately per-entry copy (`"Visit the site"` for WithinTech, `"Try the live demo"` for SubTrack) rather than a generic default, matching `content-spec.md` exactly.
  - **New `src/components/ui/ArrowLink.tsx`** — the "arrow link" / "external link" pattern `ui-rules.md` specifies as shared across the Ledger, case-study pages, and the writing index (`"Arrow link (case study, all posts)"`). Built once now since it's already documented as a cross-page pattern, not a hypothetical. `→` for internal (`next/link`), `↗` for external (`target="_blank" rel="noopener noreferrer"`); arrow is always `--accent`, text is `--text-primary` and turns `--accent` on hover, arrow translates via Tailwind v4's native `translate` utility (not `transform` — confirmed by computed-style check, since `transform` reads `none` even while hovered in Tailwind v4's CSS-property-based translate implementation).
  - **Entry number + rule** (`── 01 ──…`) built as a three-part flex row (short rule segment, mono number in `--accent`, rule segment filling the remainder) rather than a background-image or pseudo-element trick — matches `ui-rules.md`'s "number set in mono, inline with the rule, at the left" literally and stays a plain Server Component.
  - **Desktop grid is `[14ch_1fr_1fr]`** (margin figures / Built / Proves), collapsing to a single stacked column below `768px` per `ui-rules.md`'s Ledger spec (figures → Built → Proves, rule and number preserved). Margin figures render as an unstyled `<ul>` that wraps horizontally on mobile and stacks vertically at `md:`, so the four figures per entry never overflow at 320px.
  - **No visible section heading was invented beyond "The Ledger"** itself — that's the section's actual name throughout `build-plan.md`/`ui-rules.md`, not marketing copy, and gives the section a real `<h2>` landmark (heading levels: `h1` in Hero, `h2` here, no skip).
  - **Verified with Playwright**: both themes confirm the margin figures resolve to `--accent`, the `BUILT`/`PROVES` labels resolve to `--text-secondary`, and arrow-link text resolves to `--text-primary` — all matching token hex values exactly, no leaked hardcoded color. Keyboard focus reaches the ledger links with a visible outline. `prefers-reduced-motion` context confirms the section renders (no entrance motion here — the Ledger isn't in the hero's one-motion-moment scope). No horizontal overflow at 320px in either theme. JavaScript-disabled context confirms the full Built/Proves text renders. Removed the third entry via DOM manipulation as a robustness check for `build-plan.md`'s "correct with only two entries present" verify note — two-entry layout holds with no overflow. External link confirmed `target="_blank"`/`rel="noopener noreferrer"`; hover-arrow translation confirmed via the `translate` computed style. Scratch scripts and screenshots deleted after.
- **08 Case Study Pages (SubTrack + WithinTech Learn):** `src/app/work/[slug]/page.tsx` (statically generated via `generateStaticParams` off `getAllWork()`, `generateMetadata` per `architecture.md`'s documented pattern) and `src/app/work/[slug]/opengraph-image.tsx`. New `src/components/work/CaseStudyHeader.tsx` (title, summary, role · timeframe, stack chips, live/repo links per `ui-rules.md`'s header-block spec). `Container` gained an optional `size` prop (`"content" | "prose"`) so the case-study route could use the `--container-prose` (44rem) width `ui-rules.md` specifies without duplicating the padding/centering logic — `max-w-prose` resolves against the project's own `--container-prose` token (Tailwind v4's `--container-*` namespace), not the framework's unrelated built-in `65ch` default, since `globals.css` already redefines it.
  - **Wrote the full case-study bodies for SubTrack and WithinTech Learn** per `content-spec.md`'s Case Study Template (The problem / What I built / one hard problem in depth / What I'd do differently / Screenshots) and the "suggested hard problem" for each already specified there — SubTrack's renewal job on hosting that sleeps (catch-up loop, idempotent writes keyed on subscription+due-date, `paidAt` set to the due date rather than the run date), WithinTech's gated progression and progress computation (completion/pass records as the single source of truth, computed at read time rather than cached, so unlock state and percentage-complete can't drift apart). WithinTech's body opens with an explicit "Scope, stated upfront" section naming what predates Bruce (code-execution engine, challenges, auth) before describing what's his — read `profile-facts.md`'s claim-discipline note as calling for proactive disclosure, not just omission, since the risk it's guarding against is an interviewer asking about someone else's code. Restaurant-saas's MDX body is untouched (still the feature-13 placeholder) — out of scope until the client-review pass build-plan assigns to that feature.
  - **Found a real ownership-boundary problem in the existing screenshot assets, mid-feature**: `public/work/withintech-learn/light-theme.png`/`dark-theme.png` — captured for the "dark theme, same view in both themes" shot — actually show the Challenges admin list (title/difficulty/status/attempts, an admin sidebar), which `screenshot-spec.md` explicitly lists under "Never capture" and `profile-facts.md` marks as not his. Flagged to the user rather than deciding unilaterally, given the confidentiality/ownership stakes; user chose to recapture a proper pair later rather than block this feature. See the new Open Questions entry above — **do not use these two files** until replaced.
  - **Screenshots sourced from `public/work/{project}/*`** (the actual on-disk path — `screenshot-spec.md` still says `public/images/work/`, a doc/reality mismatch that predates this feature and wasn't in scope to reconcile). All `Figure` usages use each image's real pixel dimensions (checked via a one-off PowerShell `System.Drawing` script, not committed) and real, specific alt text — no "screenshot" placeholders. SubTrack's three mobile shots are wrapped in `MobileScreenshotFrame`; WithinTech has no mobile shots (web-only product).
  - **`opengraph-image.tsx` uses hardcoded light-palette hex values, not CSS custom properties** — `ImageResponse`/Satori renders outside the browser and can't read `globals.css`, and `architecture.md` already specifies OG images stay fixed to the light theme regardless of viewer preference. No custom font is loaded (Satori's default sans), which reads correctly but doesn't match Fraunces — acceptable for now since `build-plan.md` assigns the full OG/font treatment to feature 11; feature 08 only needed a real, correctly-themed, per-page image, which this is. Verified by reading the actual generated PNGs out of `out/work/{slug}/opengraph-image` after a static-export build, not just the component code.
  - **Verified with Playwright** across both pages: single `<h1>` with no skipped heading levels, zero images missing alt text, title/body colors resolve to the exact `--text-primary`/`--surface` hex in both themes, no horizontal overflow at 320px or desktop, keyboard focus reaches links with a visible outline, `prefers-reduced-motion` content stays visible, and a JS-disabled context confirms full MDX body text renders (4,400+ and 4,600+ characters respectively — not an empty shell). Visually confirmed in both themes: image frame (`--surface-raised`/`--rule`) renders correctly against `--ink` in dark mode, `MobileScreenshotFrame` scrolls horizontally inside its own container without the page overflowing at 320px. `pnpm build` confirms all three `/work/[slug]` routes plus their three `opengraph-image` routes prerender as static output. Scratch scripts and screenshots deleted after.
- **09 Writing Index + Post Pages (+ first post):** `src/app/writing/page.tsx` (ruled list grouped by year, empty-state line from `ui-rules.md` when a production build has zero non-draft posts), `src/app/writing/[slug]/page.tsx` (`generateStaticParams`/`generateMetadata` mirroring `work/[slug]`'s pattern), `src/components/writing/WritingList.tsx` and `PostHeader.tsx`, and `src/app/writing/rss.xml/route.ts`. Wrote the full body of the first post, **"Running a correct daily job on hosting that sleeps,"** and flipped its frontmatter to `draft: false` — the second launch-set post (`append-only-financial-records.mdx`) stays `draft: true`, assigned to feature 14.
  - **`PostItem`/`getPostBySlug` gained a `readingTime: number` field** in `lib/content.ts`, computed by the feature-04 `getReadingTime()` helper against the raw MDX string already available inside `readFrontmatter()` — reading time is derived data, not frontmatter, so it isn't part of the zod schema; both `getAllPosts()` (for the index) and `getPostBySlug()` (for the post page's meta row) needed their own copy of the same number, computed the same way, so there's exactly one function that knows how.
  - **RSS route is `src/app/writing/rss.xml/route.ts` with `export const dynamic = "force-static"`** — confirmed against current Next.js docs (via Context7) that `output: "export"` only supports `GET` route handlers when explicitly marked `force-static`; without it the build would either fail or silently skip the route. Hand-rolled XML (title/link/guid/description/pubDate per item, `&`/`<`/`>`/quotes escaped) rather than a feed-generation package — RSS 2.0 for four fields doesn't justify a dependency addition per `architecture.md`'s Dependency Policy. Sourced from the same `getAllPosts()` the index page renders with, so a draft never leaks into the feed and a new post appears in both automatically.
  - **Date formatting uses `Intl.DateTimeFormat` with `timeZone: "UTC"` explicitly** — `new Date("2026-07-29")` parses ISO date-only strings as UTC midnight, and formatting without pinning the timezone would shift the displayed day backward for any reader west of UTC. Same fix applied in both `WritingList` (mono `"JUL 29"` style) and `PostHeader` (full `"July 29, 2026"`).
  - **No home-page "Writing teaser" section was built in this feature** — `content-spec.md` specs its copy and `architecture.md`'s file tree lists a `WritingTeaser` component, but `build-plan.md`'s feature 09 entry itself only calls for the index, post pages, reading time, and RSS. Left out under the scope-is-sacred rule rather than assumed-in; flagging here in case it was meant to ride along and needs its own pass before feature 12.
  - **Verified with Playwright against the real static-export output** (`pnpm build` → serve `out/` with clean-URL rewriting, not `next start`, since `output: "export"` has no server build): both themes confirm exactly one `<h1>` on the index and on the post page, heading levels never skip, the post-row hover fill resolves to the exact `--accent-wash` hex in both themes (`#EFE4CD` light / `#3A2E1C` dark — checked with an explicit post-hover settle delay, since the 150ms color transition read as fully transparent if sampled at the instant of the hover event), keyboard tab reaches the post link with a visible 2px outline, no horizontal overflow at 320px on either page, `prefers-reduced-motion` leaves the post visible, and JS-disabled contexts confirm both the index list and the full post body (4,169 characters) render without JavaScript. Confirmed via the built `out/writing/rss.xml` and a production build that the draft post (`append-only-financial-records`) is absent from the index, the sitemap of routes, and the RSS feed, while the published post appears in all three; validated the RSS as well-formed XML. Scratch script, `out/`, and the temporary local server deleted/stopped after.